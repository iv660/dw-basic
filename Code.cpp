#include <string>
#include <vector>
#include "Code.h"
#include "Line.h"
#include "Output.h"

using namespace std;

// Code* Code::addLine(int number, string rawCommand)
// {
//     linesCount++;
//     Line *newLines = new Line[linesCount];
//     int lastLineIndex = linesCount - 1;
    
//     for (int i = 0; i <= lastLineIndex - 1; i++) {
//         newLines[i] = lines[i];
//     }
    
//     newLines[lastLineIndex] = * new Line(number, rawCommand);
    
//     delete[] lines;
//     lines = newLines;
    
    
    
//     Output out;
//     for (int i = 0; i <= lastLineIndex; i++) {
//         out.writeString(to_string(lines[i].number) + " " + lines[i].rawCommand);
//     }

//     return this;
// }

Code* Code::addLine(int number, string rawCommand)
{
    

    return this;
}